--- 
 - hosts: all 
   become: yes
   gather_facts: yes
   tasks: 
   - name: i want ping my all nodes 
     ping: 
   - name: create files 
     file:
      path: /home/narasimha
      state: touch
     when: ansible_os_family=="Debian"

