--- 
 - hosts: 172.31.9.197 
   become: yes
   gather_facts: yes
   tasks: 
   - name: i want ping my all nodes 
     ping: 

   - name: install apche2
     apt: 
      name: "{{item}}"
      state: present 
      loop: 
      - apache2 
      - git 
      - wget 
      - tree 
     when: ansible_os_family=="Debian"